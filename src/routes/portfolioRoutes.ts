import express from 'express';
// import { createPortfolio, validatePortfolio } from '../validations/userValidation';

export const portfolioRouter = express.Router();

// portfolioRouter.post("/createPortfolio", validatePortfolio, createPortfolio);